{
  "name": "arken",
  "version": "0.1.0",
  "scripts": {
    "commit": "npm-run-all commit:*",
    "push": "npm-run-all push:*",
    "push:evolution": "cd packages/evolution && git push && cd packages/protocol && git push && cd ../realm && git push && cd ../shard && git push",
    "push:node": "cd packages/node && git push",
    "push:cerebro": "cd packages/cerebro && git push",
    "push:shifter": "cd packages/shifter && git push",
    "push:main": "git push",
    "pull": "npm-run-all pull:*"
  },
  "dependencies": {},
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "resolutions": {}
}
